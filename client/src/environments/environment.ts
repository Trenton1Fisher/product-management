//Supabase keys are fine to show as long as row level security is setup properly according to supabase angualar setup docs
export const environment = {
  production: false,
  supabaseUrl: '',
  supabaseKey:
    '',
};
